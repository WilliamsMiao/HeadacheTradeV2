# 风控说明

仓位由风险反推：

1. 确定入场价。
2. 确定初始止损价。
3. 计算每股风险。
4. 根据账户权益和单笔风险比例计算最大亏损金额。
5. 根据单股最大仓位和组合约束取更小股数。

剧本 B 使用 `script_b_risk_multiplier` 降低风险，默认不超过剧本 A 的一半。市场为 `NEUTRAL_POSITIVE` 时使用 `neutral_risk_multiplier` 降低风险。

硬止损优先级最高，不能被结构或评分取消。滑动止盈只能收紧，不能放宽。

